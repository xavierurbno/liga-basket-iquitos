# Scripts de desarrollo local

Utilidades que antes vivían en `scratch/` (carpeta eliminada en Fase 3).

| Comando npm | Descripción |
|-------------|-------------|
| `npm run db:dev:drizzle-migrations` | Lista filas en `drizzle.__drizzle_migrations` |

Para inspección ad hoc de tablas o datos, usa `npm run studio` (Drizzle Studio) o `npm run db:verify:dev`.
