/**
 * Contenedor maestro del portal público (navbar + cuerpo):
 * misma línea vertical para logo, hero y títulos (`max-w-7xl` + gutters).
 */
export const PORTAL_SHELL_CLASS = "mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8";
