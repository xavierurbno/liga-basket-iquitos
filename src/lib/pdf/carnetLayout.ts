/** CR80 horizontal: ancho × alto en mm (ISO/IEC 7810 ID-1). */
export const CARNET_ANCHO_MM = 85.6;
export const CARNET_ALTO_MM = 53.98;

/** Slot de foto en el anverso del carnet (impresión ~300 DPI vía imagenImpresion). */
export const CARNET_FOTO_ANCHO_MM = 18;
export const CARNET_FOTO_ALTO_MM = 22;

export const CARNET_MARGEN_MM = 2.2;

/** Encabezado compacto (logos moderados, título protagonista). */
export const CARNET_HEADER_ALTO_MM = 9;
export const CARNET_HEADER_LOGO_MM = 6.5;

/** Franja inferior de acento (identidad de liga). */
export const CARNET_ACCENT_BAND_MM = 4;

/** QR reverso — composición funcional con aire. */
export const CARNET_QR_REVERSO_MM = 13;

/** Espaciado entre filas de datos en el anverso. */
export const CARNET_CAMPO_LINEA_MM = 3.75;

/** Zona de firmas en reverso. */
export const CARNET_REVERSO_FIRMAS_ALTO_MM = 12;

/** Panel blanco (foto / QR). */
export const CARNET_PANEL_RGB: [number, number, number] = [255, 255, 255];
