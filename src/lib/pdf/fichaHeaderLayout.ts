/** Caja de logos en cabecera de ficha (PDF y vista previa). */
export const FICHA_HEADER_LOGO_W_MM = 26;
export const FICHA_HEADER_LOGO_H_MM = 28;

/**
 * El PNG de federación llena casi todo el lienzo; la liga tiene más margen.
 * Escala visual dentro de la misma caja para simetría con el logo de liga.
 */
export const FICHA_HEADER_FEDERATION_LOGO_SCALE = 0.5;

/** Ajuste óptico (≈ Tailwind `pt-1`) para alinear cap height con la línea T1. */
export const FICHA_HEADER_LOGO_TOP_OFFSET_MM = 0.35;
